# Full PushT offline sequence for Anchored Local Dynamics calibration.
# Each dataset step is one LeWM coarse step = 5 raw PushT actions.
# No privileged simulator state is loaded.
dataset:
  num_steps: ${eval:'${wm.history_size} + ${ald.rollout_horizon}'}
  frameskip: 5
  name: pusht_expert_train
  keys_to_load:
    - pixels
    - action
    - proprio
  keys_to_cache:
    - action
    - proprio
