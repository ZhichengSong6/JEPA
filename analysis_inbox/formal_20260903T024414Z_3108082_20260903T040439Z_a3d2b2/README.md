# Temporary PushT analysis: formal_20260903T024414Z_3108082

Start with `transfer_manifest.json`, `paired_summary.json`, `case_metrics.csv` and `mean_execution_audit.csv`. For split files, concatenate the listed parts as UTF-8 bytes and verify the original SHA-256 before parsing.

This is a transfer of existing results, not a new experiment. Complete recordings and candidate arrays remain on the originating server. The owner requested deletion of this temporary directory after analysis.
